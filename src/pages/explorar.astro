---
import Layout from "../layouts/Layout.astro";
import NewPageWrapper from "../components/home-page/PageWrapperfil";

type PersonajeRow = {
  id_personaje: number | string;
  nombre_personaje: string;
  img_personaje: string | null;
  id_universo?: number | null;
  productsCount?: number;
};

type UniversoRow = {
  id_universo: number | string;
  nombre_universo: string;
};

type CategoryCard = {
  meta: {
    id_personaje?: number | string;
    title: string;
    slug: string;
    gallery: { url: string }[];
  };
  productsCount: number;
};

function slugify(s: string) {
  return s
    .toString()
    .normalize("NFKD")
    .replace(/[\u0300-\u036f]/g, "")
    .toLowerCase()
    .trim()
    .replace(/\s+/g, "-")
    .replace(/[^\w-]+/g, "")
    .replace(/--+/g, "-");
}

const db = Astro.locals.runtime.env.DB as any;

// ✅ lee filtro del universo
const universoId = Astro.url.searchParams.get("universoId");

// ✅ botón para limpiar filtro
const clearFilterHref = universoId ? "/explorar" : null;

// ✅ categories tipado desde el inicio
let categories: CategoryCard[] = [];

// ✅ nombre del universo para header
let universoNombre: string | null = null;

if (!db) {
  console.warn("[explorar] DB binding no disponible.");
} else {
  // ✅ si viene universoId, buscamos el nombre del universo
  if (universoId) {
    const universoRes = await db
      .prepare(`
        SELECT id_universo, nombre_universo
        FROM universos
        WHERE id_universo = ?
        LIMIT 1
      `)
      .bind(universoId)
      .first();

    const universo = universoRes as UniversoRow | null;
    universoNombre = universo?.nombre_universo ?? null;
  }

  const sql = `
    SELECT
      p.id_personaje,
      p.nombre_personaje,
      p.img_personaje,
      p.id_universo,
      COUNT(pr.id_producto) AS productsCount
    FROM personajes p
    LEFT JOIN productos pr ON pr.id_personaje = p.id_personaje
    ${universoId ? "WHERE p.id_universo = ?" : ""}
    GROUP BY p.id_personaje
    ORDER BY p.nombre_personaje ASC
  `;

  const personajesRes = universoId
    ? await db.prepare(sql).bind(universoId).all()
    : await db.prepare(sql).all();

  const personajes = (personajesRes?.results || []) as PersonajeRow[];

  categories = personajes.map((p) => ({
    meta: {
      id_personaje: p.id_personaje,
      title: p.nombre_personaje,
      slug: slugify(p.nombre_personaje),
      gallery: p.img_personaje ? [{ url: p.img_personaje }] : [],
    },
    productsCount: Number((p as any).productsCount || 0),
  }));
} // ✅ AQUÍ CIERRA EL else

// ✅ título dinámico
const meta = {
  title: universoNombre ? `Explorar: ${universoNombre}` : "Explorar Personajes",
};
---

<Layout title={`${meta.title} | 3D Shop`}>
  <NewPageWrapper
    client:load
    meta={meta}
    categories={categories}
    clearFilterHref={clearFilterHref}
  />
</Layout>